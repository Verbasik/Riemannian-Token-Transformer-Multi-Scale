---
title: "PATTERNS/[pattern-name] — паттерн [название]"
purpose: "Зафиксировать переиспользуемую инженерную технику с примером применения в этом проекте"
entrypoint: "AGENTS.md -> INDEX.md -> AREAS/*.md -> PATTERNS/[pattern].md"
authority: "controlled"
status: "active"
reads: []
writes: []
depends_on:
  - "ARCHITECTURE.md"
  - "CONVENTIONS.md"
provides:
  - "reusable_technique"
  - "implementation_guide"
canonical_owner: "Описание и применение конкретного инженерного паттерна"
last_verified: "[ГГГГ-ММ-ДД]"
max_lines: 100
---

# [Название паттерна]

Переиспользуемая инженерная техника. Загружай только когда паттерн релевантен текущей задаче.

## Contract

- when: [в каких ситуациях паттерн релевантен — конкретные типы задач или модулей]
- prereq: задача подходит под условия применения; `CONVENTIONS.md` известен
- reads: этот файл; при необходимости `AREAS/<имя>.md` для локального контекста
- writes: none
- success: понятно как применить паттерн в этом проекте с конкретными путями и импортами
- on_fail: если паттерн не подходит -> не применять; рассмотреть альтернативу или создать новый

## Canonical scope

- contains: условия применения, реализация в этом проекте, пример из кодовой базы, ловушки
- excludes:
  - общее описание паттерна в абстрактном смысле
  - реализации в других проектах

## When to apply

<!-- Конкретные условия: типы задач, модули, ситуации. Не абстрактно. -->

## Implementation

<!-- Как реализовать в ЭТОМ проекте: конкретные пути файлов, импорты, зависимости.
     НЕ общее описание — только применение здесь. -->

## Example

<!-- Минимальный пример ИЗ КОДОВОЙ БАЗЫ.
     Если такого ещё нет — псевдокод, специфичный для этого проекта. -->

## Pitfalls

<!-- 2–3 частые ошибки при применении этого паттерна в контексте ЭТОГО проекта. -->

## Constitution link

<!-- Если паттерн связан с принципом из CONSTITUTION.md — укажи какой.
     Если связи нет — удали эту секцию. -->

## Failure routes

- Если паттерн противоречит `CONVENTIONS.md` -> пересмотреть; зафиксировать в `DECISIONS.md`
- Если паттерн устарел -> пометить `status: deprecated`; предложить замену
