# Chisco training environment requirements
# Install these inside the Chisco repo's virtualenv.

# Core (required)
torch>=2.2
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
transformers>=4.40

# Optional but recommended (analysis / IO)
pandas>=2.0
matplotlib>=3.7
openpyxl>=3.1
tqdm>=4.66

# EEG preprocessing pipeline
mne>=1.6
pyprep>=0.4
mne-icalabel>=0.5
h5py>=3.9
mat73>=0.62

# Notes:
# - If torch fails to install from PyPI on your platform, you can try:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
# - These versions are compatible with Python 3.12.
