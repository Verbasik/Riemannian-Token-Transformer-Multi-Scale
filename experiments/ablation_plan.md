# План абляционных экспериментов (Phase 4B)

Цель: изолированно проверить влияние каждого изменения на f1_macro и достичь прироста минимум +0.33% (абс.) относительно текущего бейзлайна.

- Primary metric: `f1_macro` (валидация)
- Secondary: `balanced_accuracy`, `loss`, `accuracy`, `precision_macro`, `recall_macro`
- Протокол: фиксированный seed (=42), одинаковые сплиты, один фактор меняется за раз
- Оценка: усреднение по 5 фолдам (или по Fold 1 для быстрых прогонов); при наличии 5 фолдов — парный t-test на дельту F1
- Порог значимости улучшения: +0.33% (абс.)

## Наборы данных
- DS1 (baseline): `sub-04`, Fold 1 (как в текущем репо)
- DS2 (multi-subject): `sub-01..sub-04`, без агрессивного undersampling (использовать все доступные данные)
- DS3 (subject-aware CV): `StratifiedGroupKFold` (group=subject) или LOSO

Для изолированных абляций начните с DS1. После подтверждения эффекта — реплицируйте на DS2/DS3.

## Фиксированные параметры (если эксперимент явно не меняет)
- `n_epochs=50`, `batch_size` как по умолчанию, `optimizer=AdamW`, `weight_decay=1e-4`
- `scheduler=cosine`, `T_max=50`, `warmup_epochs=5` (рекомендуемая настройка для всех абляций по умолчанию)
- `normalize='zscore_hybrid'`, `exclude_channels=[124]`, `cov_type='corr'`

---

# Фаза A — Однофакторные абляции

Для каждой абляции:
- Гипотеза: кратко
- Изменение: конкретные параметры/код
- Датасет: DS1 (затем DS2/DS3 при подтверждении)
- Критерий успеха: ΔF1 ≥ +0.33% (абс.)
- Риски: возможные побочные эффекты
- Логирование: сохранить метрики в `Train/results/<exp_name>/metrics.json`

## A1 — WeightedRandomSampler (баланс в батчах)
- Гипотеза: Балансировка градиентов по классам без потери данных повышает f1_macro.
- Изменение (train loader):
  - Вычислить веса классов: `w_c = total / (num_classes * count_c)`
  - Вес сэмпла i: `weights[i] = w_c[label_i]`
  - `WeightedRandomSampler(weights, num_samples=len(train_idx), replacement=True)`
  - DataLoader(train, sampler=sampler, shuffle=False)
- Датасет: DS1
- Критерий успеха: +0.33%
- Риски: шумные миноритарные примеры могут переусилиться (компенсируется CB-Focal).

### Результаты (DS1, Fold 1)
- Early stopping: эпоха 21 (лучшая модель)
- Метрики:
  - accuracy: 0.2474
  - f1_macro: 0.2266
  - precision_macro: 0.3076
  - recall_macro: 0.2876
  - balanced_accuracy: 0.2876
  - loss: 1.4887
- Сравнение с бейзлайном (sub-04, Fold 1):
  - f1_macro: 0.2737 → 0.2266 (−0.0471)
  - accuracy: 0.2874 → 0.2474 (−0.0400)
  - balanced_accuracy: 0.2929 → 0.2876 (−0.0053)
  - loss: 1.4607 → 1.4887 (+0.0280)
- Вывод: чистое включение WeightedRandomSampler при активном Class-Balanced Focal Loss ухудшило макро-F1; прирост цели (+0.33%) не достигнут.
- Гипотезы причин:
  - Двойная балансировка (sampler + CB-Focal) переусиливает редкие/сложные примеры, увеличивая дисперсию градиентов и шум в обучении.
  - Семплирование с заменой вызывает частые повторения миноритарных сэмплов в эпохе → снижается покрытие разнообразия.
- Рекомендации:
  - R1: Отключить sampler при CB-Focal и перейти к A2 (подбор `gamma`/`beta`).
  - R2: Смягчить sampler: `weights_per_class **= alpha`, где `alpha∈[0.5,0.8]`; или смешивание с uniform: `w = λ·w + (1−λ)` с `λ∈[0.5,0.8]`.
  - R3: Если оставлять sampler — заменить CB-Focal на CrossEntropy (или снизить `gamma≈1.25`).
- Решение: перейти к A2; подготовить ветку A1' с soft-sampler (`alpha=0.5`) для повторной проверки.

## A2 — CB-Focal настройка (`gamma`, `beta`)
- Гипотеза: Слегка меньшие `gamma`/`beta` уменьшат переакцент на hard-примерах и стабилизируют обучение.
- Изменение: сетка `gamma ∈ {1.25, 1.5, 1.75}`, `beta ∈ {0.999, 0.9995, 0.9999}`
- Датасет: DS1
- Критерий: +0.33%
- Риски: слишком малый `gamma` ухудшит внимание к сложным примерам.

### Результаты (DS1, Fold 1)
- Комбинация: `gamma=1.25`, `beta=0.999`
- Обучение: ранняя остановка на эпохе 21 (лучшая модель восстановлена)
- Метрики:
  - accuracy: 0.2770
  - f1_macro: 0.2734
  - precision_macro: 0.2628 (из лога указана как Val F1; уточнить precision в полном выводе)
  - recall_macro: — (в полном выводе метрик)
  - balanced_accuracy: — (в полном выводе метрик)
  - loss: 1.5147 (Val Loss на лучшей эпохе)
- Сравнение с бейзлайном (Fold 1):
  - f1_macro: 0.2737 → 0.2734 (−0.0003, −0.03% абс.) — практически на уровне бейзлайна
- Вывод: комбинация `gamma=1.25`, `beta=0.999` не даёт прироста ≥ +0.33%, но избегает деградации, наблюдаемой при A1.

### Следующие комбинации (приоритет)
- `gamma ∈ {1.5, 1.75}`, `beta ∈ {0.9995, 0.9999}` — провести короткие прогоны (20–25 эпох), затем лучшие — 50 эпох.
- Если разброс метрик высок: зафиксировать `T_max=50, warmup_epochs=5` во всех запусках A2 для стабильности.

#### Лучший результат A2 (обновление)
- Комбинация: `gamma=1.75`, `beta=0.999`
- Метрики:
  - accuracy: 0.3026
  - f1_macro: 0.2908
  - precision_macro: 0.2944
  - recall_macro: 0.3037
  - balanced_accuracy: 0.3037
  - loss: 1.4384
- Сравнение с бейзлайном (Fold 1):
  - f1_macro: 0.2737 → 0.2908 (+0.0171, +1.71% абс.) ✓ Превышает порог +0.33%
  - accuracy: 0.2874 → 0.3026 (+0.0152)
  - balanced_accuracy: 0.2929 → 0.3037 (+0.0108)
  - loss: 1.4607 → 1.4384 (−0.0223)
- Вывод: A2 достиг целевого улучшения. CB-Focal с `gamma=1.75`, `beta=0.999` — текущий победитель на DS1.
- Действия:
  - D1: Зафиксировать `gamma=1.75`, `beta=0.999` в `config.py` (после подтверждения).
  - D2: Реплицировать результат на 5-фолдах DS1 и на DS2 (без undersampling) для проверки устойчивости.
  - D3: Перейти к A4 (gating) и A5 (attn_heads=2) как следующая ступень (Фаза B: B2/B3).

## A3 — Расписание LR (Cosine + warmup)
- Гипотеза: Согласование периода с длиной обучения и мягкий старт повышают итоговую метрику.
- Изменение: `scheduler={'name': 'cosine', 'T_max': 50, 'warmup_epochs': 5}`
- Датасет: DS1
- Критерий: +0.33%
- Риски: дольше выход на «полезную» скорость — не критично при 50 эпохах.

## A4 — Gating между масштабами
- Гипотеза: Адаптивное взвешивание токенов разных масштабов улучшит разделимость.
- Изменение: `model.gating=True`
- Датасет: DS1
- Критерий: +0.33%
- Риски: небольшое увеличение параметров и риска переобучения (компенсируется регуляризацией).

## A5 — Количество голов в pooling (`attn_heads`)
- Гипотеза: Больше голов увеличивает выразительность агрегации токенов.
- Изменение: `model.attn_heads ∈ {1, 2, 4}` (базу сравнить с 1)
- Датасет: DS1
- Критерий: +0.33%
- Риски: рост вычислений и возможная нестабильность при больших головах.

## A6 — Subject embeddings: размерность и регуляция
- Гипотеза: Увеличение размерности до 32/64 + лёгкая регуляция повышают переносимость.
- Изменение: `subject_embed_dim ∈ {16, 32, 64}`; добавить Dropout(0.1–0.2) на эмбеддинг (если потребуется) или L2.
- Датасет: DS1 → DS2
- Критерий: +0.33%
- Риски: переобучение при большой размерности без регуляции.

## A7 — Плотность окон (stride_small)
- Гипотеза: Более частая дискретизация окна повышает полноту признаков.
- Изменение: `stride_small ∈ {96 (base), 80, 64}`
- Датасет: DS1
- Критерий: +0.33%
- Риски: рост времени и потребления памяти.

## A8 — SPD-аугментация
- Гипотеза: Малый шум в касательном пространстве повышает робастность.
- Изменение: `use_spd_augment=True`, `spd_jitter_std ∈ {0.02, 0.03}`, `spd_jitter_prob ∈ {0.2, 0.3}`
- Датасет: DS1
- Критерий: +0.33%
- Риски: избыточный шум снижает signal-to-noise.

## A9 — EMA параметров (Polyak averaging)
- Гипотеза: Сглаживание параметров уменьшает вариативность финальной точки.
- Изменение: EMA поверх весов c `alpha ∈ {0.99, 0.995}`; валидировать EMA-снэпшот.
- Датасет: DS1
- Критерий: +0.33%
- Риски: неверный decay может замедлять адаптацию.

## A10 — Тип SPD-нормировки: `cov_type`
- Гипотеза: `trace`-нормировка может быть полезна при нестабильности корреляционной матрицы.
- Изменение: `cov_type ∈ {'corr', 'trace'}`
- Датасет: DS1
- Критерий: +0.33%
- Риски: потеря относительных масштабов корреляций.

## A11 — Subject embeddings: включение/выключение
- Гипотеза: Без эмбеддингов может улучшиться перенос на новые субъекты; с эмбеддингами — выше качество в замкнутом множестве.
- Изменение: `use_subject_embed ∈ {True, False}`
- Датасет: DS1 → DS2/DS3
- Критерий: +0.33%
- Риски: падение на мультисубъектных данных при отключении.

## A12 — Домашний baseline балансировки (для справки)
- Гипотеза: Агрессивный undersampling ухудшает F1 — подтверждено эмпирически.
- Изменение: НЕ применять undersampling; оставлено для фиксации анти-паттерна.
- Датасет: DS2
- Критерий: — (диагностический эксперимент)
- Риски: —

## A13 — Subject-aware CV (оценка)
- Гипотеза: Групповая стратификация даёт честную оценку переносимости.
- Изменение: `StratifiedGroupKFold` (group=subject) и/или LOSO
- Датасет: DS2/DS3
- Критерий: — (изменение протокола оценки), сравнить с обычным K-Fold
- Риски: возможное падение метрик — но это честная оценка.

---

# Фаза B — Комбинации (после A1–A11)

- B1: A1 (Sampler) + лучшая комбинация A2 (CB-Focal)
- B2: B1 + A4 (Gating)
- B3: B2 + A5 (attn_heads=2)
- B4: B3 + A8 (SPD-augment) — при положительном эффекте
- B5: B3 + A9 (EMA) — альтернативный путь стабилизации

Комбинации запускать только после фиксации единичных эффектов. Цель — проверить аддитивность улучшений.

---

# Реализация: подсказки по коду

## WeightedRandomSampler (скetch)
```
# после получения train_idx, val_idx
from torch.utils.data import WeightedRandomSampler
labels = np.array([s['label'] for s in samples])
train_labels = labels[train_idx]
class_counts = np.bincount(train_labels, minlength=cfg['model']['n_classes'])
weights_per_class = (len(train_labels) / (len(class_counts) * np.clip(class_counts, 1, None)))
weights = weights_per_class[train_labels]
sampler = WeightedRandomSampler(weights=torch.DoubleTensor(weights), num_samples=len(train_idx), replacement=True)
train_loader = DataLoader(ChiscoSubset(dataset, train_idx), batch_size=cfg['training']['batch_size'], sampler=sampler, shuffle=False, num_workers=cfg['training']['num_workers'], pin_memory=cfg['training']['pin_memory'])
```

## EMA (скetch)
```
# инициализация
ema = {k: v.detach().clone() for k, v in model.state_dict().items()}
alpha = 0.99
# после каждого optimizer.step()
with torch.no_grad():
    for k, v in model.state_dict().items():
        ema[k].mul_(alpha).add_(v, alpha=1 - alpha)
# для валидации (опция):
backup = {k: v.detach().clone() for k, v in model.state_dict().items()}
model.load_state_dict(ema)
val_metrics = evaluate(...)
model.load_state_dict(backup)
```

---

# Логирование и нэйминг экспериментов
- Формат: `Train/results/ablations/<A|B><номер>_<ключевые_параметры>/metrics.json`
- Примеры:
  - `A1_sampler_on/metrics.json`
  - `A2_focal_gamma1.5_beta0.9995/metrics.json`
  - `A4_gating_true/metrics.json`

Сохранять также конфиг запуска рядом: `config.json` (дамп итогового словаря cfg) для воспроизводимости.

---

# Критерии завершения
- Абляция успешна при ΔF1 ≥ +0.33% (абс.) на DS1; при подтверждении — воспроизвести на DS2/DS3
- Комбинированные эксперименты (Фаза B) должны сохранять or повышать улучшение

---

# Примечания по рискам и интерпретации
- Малые улучшения на одном фолде могут быть нестабильны: подтверждать 5-fold и/или subject-aware CV
- SPD-аугментация и EMA повышают стабильность, но требуют аккуратной калибровки
- Изменение плотности окон влияет на память/время — контролировать batch_size
- При мультисубъектной оценке предпочтительны subject-aware протоколы (StratifiedGroupKFold/LOSO)
