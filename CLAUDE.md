
# Claude Code — точка входа

Читай и выполняй все инструкции из @AGENTS.md.
Читай @memory-bank/INDEX.md для навигации по памяти проекта.

## Специфика Claude Code

- Skills для работы с памятью: `.claude/skills/`.
- Субагент `memory-explorer` для глубокого поиска по истории.
- Auto-memory (`/memory`) — для личных предпочтений. Проектные знания → `memory-bank/`.
- Path-scoped rules в `.claude/rules/` активируются автоматически.

