# Активный контекст

## Текущий фокус
- Инициализация Банка Памяти и фиксация текущего состояния пайплайна Phase 4B.
- Бейзлайн: обучение `sub-04` (Fold 1), гибридная нормализация, subject embeddings включены.
- Эксперимент A8 — SPD-аугментация: подготовлен скрипт свипа (`std ∈ {0.02, 0.03}`, `prob ∈ {0.2, 0.3}`) с лучшими базовыми настройками.
- Сформирован V2 план абляций (серии B0–B5) в `experiments/ablation_plan_V2.md`.
- Начата реализация B2 — ковариации SPD: добавлен Ledoit–Wolf и параметр `oas_min_alpha`; подготовлен 5-fold скрипт `experiments/run_b2_cov.py`.
- Подготовлен документ по архитектурным сдвигам C1–C3: `experiments/architectural_shifts.md` (приоритетная дорожная карта изменений модели).
 - Реализация C1 начата: добавлена опция `use_spdnet` и список `spdnet_dims` в модель; вставлен блок BiMap+ReEig до `spd_logm`. Подготовлен свип `experiments/run_c1_spdnet.py`.
 - Реализация C1b начата: добавлен ортонормальный слой проекции в тангенциальном пространстве (`use_tangent_ortho`, `tangent_ortho_dim`) и свип `experiments/run_c1b_tangent_ortho.py`.
 - Реализация C2 начата: добавлены `use_gcn`, `gcn_k`, `gcn_alpha` и C2‑свип `experiments/run_c2_gcn.py` (k∈{6,8}, α∈{0.2,0.4}). Исправлена ошибка в умножении графа: `einsum('cd,bdt->bct')` вместо неверного `'cc,bct->bct'` (раньше применялся trace(A)·X, что давало одинаковые метрики для всех k, α).
 - Реализация C3 начата: добавлена GRL‑голова для subject adversarial и CORAL‑штраф; параметры: `da_lambda`, `coral_lambda`, `domain_hidden`; флаг `use_c3`.

## Последние результаты
- `python3 Pipeline/train.py` — ✅ восстановлена лучшая модель, метрики валидации (Fold 1):
  - accuracy: 0.2874
  - f1_macro: 0.2737
  - precision_macro: 0.2770
  - recall_macro: 0.2929
  - balanced_accuracy: 0.2929
  - loss: 1.4607
  Артефакты: `Train/checkpoints/phase4b_5subjects_CPU/best_model.pt`, `Train/results/phase4b_5subjects_CPU/metrics.json`.

- A8 (SPD-аугментация, свип std×prob) — ✅ результаты (DS1, Fold 1):
  - Лучшее: `std=0.03`, `prob=0.2` → f1_macro=0.2761, accuracy=0.2978, loss=1.3897.
  - Тренды: меньшая вероятность (0.2) стабильнее 0.3 по f1; при 0.2 умеренный шум (0.03) лучше небольшого (0.02).

- B1 (Losses vs дисбаланс) — ✅ 5-fold DS1:
  - CB-Focal (γ=1.75, β=0.999): f1_macro=0.2732 (mean), acc=0.2942, loss=1.3944.
  - LA-CE / BSCE: f1_macro=0.2602, acc=0.2801, loss=1.9123 (идентичные — ожидаемая эквивалентность z+log π).
  - LDAM-DRW: s=16 → f1_macro=0.2618; s=32 → f1_macro=0.2591; loss значительно выше (масштаб s влияет на CE).
  - Вывод: CB-Focal остаётся лидером на DS1; LA-CE/BSCE/LDAM не дают прироста macro‑F1.

- B2 (SPD ковариации, Fold 1) — ✅ результаты:
  - OAS base (minα=0.1): f1_macro=0.2915, acc=0.3090, bal_acc=0.3055, loss=1.4058.
  - OAS minα=0.01: f1_macro=0.2853, acc=0.3034, bal_acc=0.3045, loss=1.4390.
  - Ledoit–Wolf: f1_macro=0.2930, acc=0.3066, bal_acc=0.3080, loss=1.4164.
  - Вывод: LW дал +0.0016 к macro‑F1 относительно OAS base на Fold 1 (в пределах шума); слишком слабый clamp (0.01) ухудшает качество. Требуется 5‑fold.

- C1 (SPDNet-вставка, Fold 1) — ✅ результаты:
  - dims=[16]: f1_macro=0.2706, acc=0.2890, loss=1.3941.
  - dims=[20,16]: f1_macro=0.2849, acc=0.3058, loss=1.3701, precision_macro=0.3215.
  - dims=[32,24,16]: f1_macro=0.1988, acc=0.2434, loss=1.4135 — сильная деградация.
  - Новые (после ослабления residual до последнего слоя, α∈{0.05,0.1}):
    - dims=[24], α=0.05 → f1_macro=0.2598, acc=0.2722, loss=1.3842.
    - dims=[24], α=0.10 → f1_macro=0.2635, acc=0.2882, loss=1.3810.
    - dims=[24,16], α=0.05 → f1_macro=0.2723, acc=0.2930, loss=1.3656.
    - dims=[24,16], α=0.10 → f1_macro=0.2656, acc=0.2914, loss=1.3604.
  - Дополнительно (α=0, без residual):
    - dims=[24,16], α=0 → f1_macro=0.2742, acc=0.2994, loss=1.3620, recall=0.2940, bal_acc=0.2940 — лучший среди SPDNet, но ниже базы (≈0.2915).
 - C1b (tangent ortho, tan_dim∈{192,160,128}):
    - 192 → f1=0.2607, acc=0.2890, loss=1.3859, prec=0.3074, rec=0.2781.
    - 160 → f1=0.2662, acc=0.2954, loss=1.3699, prec=0.3020, rec=0.2851.
    - 128 → f1=0.2623, acc=0.2826, loss=1.3995, prec=0.3061, rec=0.2757.

- C2 (GCN по электродам, tanh):
  - k=6, α=0.2 → f1=0.2674, acc=0.2858, loss=1.3924.
  - k=6, α=0.4 → f1=0.2656, acc=0.2850, loss=1.3981.
  - k=8, α=0.2 → f1=0.2678, acc=0.2890, loss=1.4007.
  - k=8, α=0.4 → f1=0.2704, acc=0.2914, loss=1.3878 (лучший среди C2).
  - Learnable GraphConv (C2a: k=8, σ=0.05, K=2, layers=1, tanh, BN): f1=0.2609, acc=0.2738, loss=1.3916 — ниже базы и фиксированного C2.
  - Learnable GraphConv (C2a v2: k=8, σ=0.05, K=1, layers=2, ReLU, LayerNorm, dropout=0.1, Chebyshev): f1=0.2736, acc=0.2922, loss=1.4420.
  - Вывод: конфигурация [32,24,16] приводит к изотропизации представлений и падению macro‑F1. Причины: тройная каскадная BiMap с residual→I (α=0.3) чрезмерно сглаживает спектр и подавляет дискриминационную структуру. План: уменьшить α (≤0.1), применять residual только в последнем слое (реализовано), протестировать мягкие dims ([24], [24,16]) с `alpha∈{0.05,0.1}`, затем 5‑fold.
  - Глубокий анализ новых прогонов: 
    - Малый α (0.05) даёт лучший F1 для [24,16] (0.2723), но всё ещё ниже базы (≈0.2915); loss заметно снижается (≈1.3656), а precision умеренный — индикатор переуверенности без выигрыша F1.
    - Увеличение α до 0.10 систематически повышает precision и снижает recall (и F1), что согласуется с гипотезой о сглаживании спектра и потерях по миноритарным классам.
    - α=0 слегка повышает F1 и recall относительно α>0, при этом сохраняет низкий loss — residual→I даже в последнем слое вреден.
    - Один слой [24] хуже, чем [24,16], что говорит о пользе лёгкого понижения размерности, но без residual‑смешений.
    - C1b улучшает loss (особенно при tan_dim=160), но macro‑F1 остаётся ниже базы и лучших SPDNet; ортонормальная проекция без явной дискриминации отбрасывает полезные компоненты.
    - C2 даёт небольшой сдвиг по loss/acc, но macro‑F1 остаётся < базы; более сильное сглаживание (α=0.4) и k=8 выглядят предпочтительнее, однако требуется усложнить слой (learnable GraphConv, расстояние‑взвешенные ребра, multi‑hop) для потенциального роста F1.
    - C2a (learnable GraphConv) при K=2, 1 слое и tanh показал падение F1 (≈0.2609). Ослабление фильтра (K=1), добавление 2 слоёв, ReLU+LayerNorm и dropout=0.1 (Chebyshev) подняло F1 до 0.2736 (ниже базы 0.2915), но увеличило val loss (1.442). Вероятные причины: регуляризация/нормализация привела к более «жёстким» постериорам без выигрыша в balance. Дальше: аккуратный свип по σ,k, K∈{0,1}, нормам (LN/BN), позиционированию граф‑блока и residual‑масштабу; оставить use_gcn=False в дефолте.

## Предлагаемые следующие шаги
- Развернуть оценку на все 5 фолдов (для `sub-04`) и усреднить метрики.
- Провести абляции: отключить `use_subject_embed`, попробовать `cov_type='trace'`, включить `gating`, варьировать `proj_channels/d_model/n_layers`.
- Подбор гиперпараметров (lr, T_max, beta/gamma в CB-Focal, окна/шаги).
- Расширить список субъектов (мультисубъектные эксперименты), оценить переносимость.
- Добавить логгинг экспериментов (локально) и сбор обучающих кривых.

## Обновление 2025-12-10 — Сбалансированный датасет 4 субъектов
- Сконструирован идеально сбалансированный по метаклассам датасет из `sub-01..sub-04`:
  - Всего: 11,464 образца; 1,433 на класс; дисбаланс 1.00x; удалено 51.9% (12,387 из 23,851).
  - Исходное распределение по субъектам: sub-01: 6,083; sub-02: 6,395; sub-03: 5,132; sub-04: 6,241; всего 23,851.
  - Финальное распределение по классам (идеально 12.5% каждый).
- Наблюдение: обучение на этом датасете дало худший результат, чем обучение только на `sub-04`.

### Анализ причин (гипотезы)
- Межсубъектная гетерогенность (subject/covariate shift) повысилась, а механизмы выравнивания доменов (кроме subject embeddings и гибридной нормализации) не применялись.
- Агрессивный undersampling до идеального баланса лишает модель богатства частых классов и внутрисубъектной/внутрисессионной вариативности, снижается устойчивость представлений.
- Стратификация по меткам без учёта групп (subjects) может давать нестабильные разбиения; нужна subject-aware CV.
- Class-Balanced Focal уже адресует дисбаланс; дополнительный сильный undersampling может избыточно усложнять обучение без выигрыша.
- Параметры персонализации (размерность subject embeddings, dropout, регуляции) могли быть неоптимальны для мультисубъектного сценария.

### План действий (приоритет)
1) Убрать агрессивный undersampling: использовать все данные и опираться на `ClassBalancedFocalLoss`/`WeightedRandomSampler` для балансировки.
2) Ввести `StratifiedGroupKFold` (группа = subject) и режим LOSO для честной оценки межсубъектной обобщаемости.
3) Провести абляции subject embeddings (on/off, размерность 16→32/64) и проверить влияние на мультисубъектных данных.
4) Рассмотреть domain alignment (например, CORAL-потеря на ковариациях) или DANN как POC.
5) Режим батч-семплинга с смешением субъектов в каждом батче для обучения инвариантностей.
6) Проверить согласованность каналов/монтажа между субъектами и корректность `exclude_channels`.

## Обновление 2025-12-11 — Результаты абляций A1–A7 (DS1: sub-04)
- A1 (WeightedRandomSampler): ухудшение F1 (0.2266) при CB-Focal → отключён по умолчанию; избегать двойной балансировки.
- A2 (CB-Focal, gamma/beta): лучшая комбинация `gamma=1.75`, `beta=0.999` (Fold1 f1=0.2908; 5-fold mean f1=0.2686). Зафиксировано в конфиге.
- A3 (Cosine LR): на DS1 лучший режим соответствует A2 (`T_max=20`, `warmup=3`); длинные периоды/большой warmup хуже. Зафиксировано в конфиге.
- A4/A5 (Gating/attn_heads): gating не улучшает f1_macro; `attn_heads=1` лучший при отключённом gating. Комбинации с heads=2 дают +0.32% к A4, но ниже A2.
- A5' (Multi-head only): heads=1 > heads=2/4 по f1; оставить `attn_heads=1` по умолчанию.
- A6 (Subject embeddings регуляция): лучший кандидат — `subject_embed_dim=16`, `dropout=0.2`, `embed_wd=5e-4` (f1≈0.2915). Зафиксировано как дефолт (до 5-fold подтверждения).
- A7 (stride_small): уменьшение шага до 80/64 ухудшает f1; оставить `stride_small=96`.

Итоговая лучшая конфигурация (DS1 Fold1):
- Loss: CB-Focal (`gamma=1.75`, `beta=0.999`).
- Scheduler: Cosine (`T_max=20`, `warmup=3`).
- Model: `gating=False`, `attn_heads=1`, subject embeddings `dim=16`, `dropout=0.2`.
- Optimizer: отдельный `subject_embed_weight_decay=5e-4`.
- Data: `normalize=zscore_hybrid`, `exclude_channels=[124]`. `stride_small=96`.

Следующие шаги:
- 5-fold валидация лучшей конфигурации на DS1 и DS2 (без undersampling).
- Подтвердить A8 (std=0.03, prob=0.2) на 5-fold DS1; затем проверить на DS2.
- Запустить серии B0–B5 согласно `ablation_plan_V2.md` (приоритет: B0→B1→B2→B3→B4→B5).
  - После B1: перейти к B2 (ковариации OAS minα=0.01, Ledoit‑Wolf), затем B3 (DropToken).
  - Для B2: провести 5-fold DS1 сравнение OAS base vs OAS min0.01 vs LW; при подтверждении — перенос на DS2.
 - A9 (EMA) как кандидат для дальнейшего прироста.
