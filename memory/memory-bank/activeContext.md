# Активный контекст

Статус: инициализирован Банк Памяти на основе анализа кода в папке `Pipeline`.

Текущий фокус
- ✓ Зафиксирована архитектура и workflow DL/ML пайплайнов.
- → Подготовка эксплуатационной документации (README, требования окружения).
- → Синхронизация dry‑run скрипта с текущими сигнатурами билд‑функций.

Риски/ограничения
- Данные ожидаются по путям `Chisco/...` вне репозитория — без них обучение не запустится.
- Отсутствует `README.md` в корне текущего репозитория.

Предлагаемые следующие шаги
1) Добавить `README.md` с инструкциями запуска и требованиями (PyTorch, зависимости, структура данных).
2) Добавить `requirements.txt`/`environment.yml` для воспроизводимой установки.
3) Обновить `test_dryrun.py` под сигнатуры `build_loaders`/`build_model` (учёт `n_subjects`).
4) Проверить/задокументировать расположение `metaclasses.json`, `classnumber.json`, `textmaps.json`.
5) (Опционально) Добавить CLI‑аргументы к `train.py` для подмены ключевых параметров без правки кода.

Дата обновления: 2025‑12‑03

